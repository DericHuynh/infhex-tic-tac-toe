{
    "name": "@ih3t/shared",
    "version": "0.0.0",
    "private": true,
    "files": [
        "src/**/*"
    ],
    "main": "src/index.ts",
    "scripts": {
        "build": "tsc --build",
        "dev": "tsc --build --watch",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}